# ==========================================
# Zuhaoku 环境变量配置示例
# ==========================================
# 复制此文件为 .env 并填写实际值
# 注意：不要将 .env 文件提交到代码仓库

# ==========================================
# 数据库配置（必需）
# ==========================================
# 本地开发使用（Docker Compose）：
# MYSQL_HOST=localhost
# MYSQL_PORT=3306
# MYSQL_DATABASE=zhk_rental
# MYSQL_USER=zhk_user
# MYSQL_PASSWORD=zhk_password
# 生产环境使用（云数据库）：
MYSQL_HOST=
MYSQL_PORT=3307
MYSQL_DATABASE=
MYSQL_USER=
MYSQL_PASSWORD=

# ==========================================
# 支付宝配置（必需）
# ==========================================
# 支付宝应用ID（从支付宝开放平台获取）
ALIPAY_APP_ID=

# 支付宝网关地址（生产环境）
ALIPAY_GATEWAY=https://openapi.alipay.com/gateway.do

# 支付宝私钥（优先使用 PKCS#1 格式，推荐）
# 格式：-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----
# 注意：环境变量中换行符使用 \n 表示
ALIPAY_PRIVATE_KEY_PKCS1=

# 或者使用 PKCS#8 格式（备用，如果设置了 ALIPAY_PRIVATE_KEY_PKCS1 则不需要）
# 格式：-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----
# ALIPAY_PRIVATE_KEY=

# 支付宝公钥（Base64 字符串，不含 BEGIN/END 标记）
# 从支付宝开放平台下载
ALIPAY_PUBLIC_KEY=

# 支付宝回调地址（必需，必须是公网可访问的 HTTPS URL）
# 本地开发：使用内网穿透工具（如 ngrok）或跳过回调测试
# 生产环境：
# 异步通知地址（支付宝服务器回调，用于更新订单状态）
ALIPAY_NOTIFY_URL=https://zuhaoku.fun/api/v1/payments/alipay/notify
# 同步跳转地址（用户支付完成后跳转到前端页面）
ALIPAY_RETURN_URL=https://zuhaoku.fun/tenant/orders

# ==========================================
# 应用配置（必需）
# ==========================================
# 服务器端口（Zeabur 会自动设置，本地开发使用 8080）
PORT=8080

# Spring 环境
# 本地开发使用：dev
# 生产环境使用：prod
SPRING_PROFILES_ACTIVE=prod

# JWT 密钥（生产环境必须修改为随机字符串，至少 32 字符）
JWT_SECRET=mljY2F1FUrqbrd1Iz81ofA0AIi/WmX5nyffZwldRYT8=

# 加密密钥（生产环境必须修改为随机字符串，至少 32 字符）
ENCRYPTION_KEY=uTYpJpiUmDtFHf4D/fA1w3G9YuWNlUhrFszRDsV7Tfk=

# 主加密密钥（生产环境必须修改为随机字符串，至少 32 字符）
ENCRYPTION_MASTER_KEY=9a7PA6kMwWSmk1m2uqWOhbBp4eTwis6UTV+IEbvFajM=

# 业务 URL（必需）
# 本地开发使用：
# BACKEND_URL=http://localhost:8080
# FRONTEND_URL=http://localhost:3000
# 生产环境使用：
BACKEND_URL=https://zuhaoku.fun
FRONTEND_URL=https://zuhaoku.fun

# ==========================================
# Redis 配置（可选）
# ==========================================
# 如果不配置 Redis，应用会禁用 Redis 相关功能
# 本地开发使用：
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=
# 生产环境使用：
REDIS_HOST=
REDIS_PORT=6379
REDIS_PASSWORD=

# ==========================================
# MinIO 配置（可选）
# ==========================================
# 如果不配置 MinIO，文件上传功能将不可用
# 本地开发使用（Docker Compose）：
# MINIO_ENDPOINT=http://localhost:9000
# MINIO_ACCESS_KEY=minioadmin
# MINIO_SECRET_KEY=minioadmin123
# MINIO_BUCKET=zhk-evidence
# 生产环境使用：
MINIO_ENDPOINT=
MINIO_ACCESS_KEY=
MINIO_SECRET_KEY=
MINIO_BUCKET=zhk-evidence
